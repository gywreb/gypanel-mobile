import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const ProductCreate = () => {
  return (
    <SafeAreaView>
      <Text>Product Create</Text>
    </SafeAreaView>
  );
};

export default ProductCreate;
